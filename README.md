# بوت تلغرام متطور لتحميل Spotify 🚀

هذا البوت مبرمج بلغة Python باستخدام مكتبة `aiogram 3.x` ويوفر ميزات إدارية متكاملة مع إمكانية تحميل الصوتيات من Spotify.

## المميزات 🌟
- **تحميل Spotify:** تحميل الأغاني بمجرد إرسال الرابط.
- **الاشتراك الإجباري:** نظام متطور يمنع استخدام البوت إلا بعد الانضمام لقناتك.
- **لوحة تحكم المطور:**
  - تعديل رسالة الترحيب مباشرة من التلغرام.
  - تغيير قناة الاشتراك الإجباري.
  - تفعيل/تعطيل إشعارات الدخول والحظر.
  - إحصائيات دقيقة (عدد الأعضاء، النشطين، المحظورين).
- **نظام الإذاعة:** إرسال رسائل لجميع مستخدمي البوت بضغطة زر.
- **قاعدة بيانات SQLite:** حفظ بيانات المستخدمين والإعدادات بشكل دائم.

## المتطلبات 🛠️
- Python 3.9+
- مكتبات: `aiogram`, `aiosqlite`, `spotdl`
- أداة `ffmpeg` (ضرورية لـ spotdl لتحويل الملفات).

## كيفية التثبيت والتشغيل 🚀

1. **تثبيت المكتبات المطلوبة:**
```bash
pip install aiogram aiosqlite spotdl
```

2. **تثبيت FFmpeg:**
- على لينكس (Ubuntu): `sudo apt update && sudo apt install ffmpeg`
- على ويندوز: قم بتحميله من الموقع الرسمي وأضفه إلى الـ PATH.

3. **إعداد الإعدادات:**
- افتح ملف `config.py`.
- ضع توكن بوتك من @BotFather في `BOT_TOKEN`.
- ضع معرفك العددي في `ADMIN_ID`.

4. **تشغيل البوت:**
```bash
python main.py
```

## هيكلية المشروع 📁
- `main.py`: نقطة انطلاق البوت.
- `config.py`: ملف الإعدادات والتوكنات.
- `database/`: يحتوي على كود التعامل مع SQLite.
- `handlers/`: يحتوي على منطق الأوامر (البدء، الأدمن، سبوتيفاي).
- `middlewares/`: يحتوي على نظام التحقق من الاشتراك.
- `data/`: يحتوي على مخطط قاعدة البيانات.

## الأمان 🔒
- لا تشارك ملف `config.py` أو قاعدة البيانات مع أي شخص.
- تأكد من إضافة `data/bot_database.db` إلى ملف `.gitignore` إذا كنت ترفع الكود على GitHub.

---
**تم التطوير بواسطة Manus**
