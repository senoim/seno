# إرشادات نشر بوت تلغرام Spotify على Railway 🚀

تم تجهيز مشروع بوت تلغرام Spotify الخاص بك للنشر السلس على منصة Railway. تضمن هذه الإرشادات تثبيت جميع التبعيات الضرورية، بما في ذلك `ffmpeg`، وإدارة المتغيرات البيئية بشكل آمن.

## 1. إعداد المشروع على Railway

1.  **إنشاء مشروع جديد**: قم بتسجيل الدخول إلى حسابك في Railway وانقر على `New Project`.
2.  **النشر من GitHub (موصى به)**: أفضل طريقة هي ربط مستودع GitHub الخاص بك بـ Railway. قم برفع الكود المحدث (الذي يحتوي على `Dockerfile` و `requirements.txt` و `.dockerignore`) إلى مستودع GitHub خاص بك.
    -   اختر `Deploy from GitHub Repo`، ثم حدد المستودع الخاص بك.
    -   سيقوم Railway تلقائياً باكتشاف `Dockerfile` وبدء عملية البناء.
3.  **النشر اليدوي (إذا لم تستخدم GitHub)**:
    -   اختر `Deploy from Git Repo` ثم `Deploy from a private Git repository` إذا كان لديك رابط لمستودع خاص.
    -   بدلاً من ذلك، يمكنك اختيار `Deploy from a local Git repository` إذا كنت تريد النشر مباشرة من جهازك بعد إعداد Git.

## 2. إعداد المتغيرات البيئية (Environment Variables)

بعد إنشاء المشروع، ستحتاج إلى إضافة المتغيرات البيئية الضرورية:

1.  **انتقل إلى صفحة المشروع**: في لوحة تحكم Railway، اختر المشروع الذي أنشأته.
2.  **أضف المتغيرات**: اذهب إلى تبويب `Variables` وأضف المتغيرات التالية:
    -   `BOT_TOKEN`: توكن البوت الخاص بك الذي حصلت عليه من @BotFather.
    -   `ADMIN_ID`: معرف التلغرام العددي الخاص بك كمطور (لإرسال الإشعارات ولوحة التحكم).
    -   `DB_PATH`: (اختياري) يمكنك تركه افتراضياً `data/bot_database.db`، أو تحديده لمسار آخر إذا كنت تستخدم تخزيناً دائماً خارجياً (Persistent Volume) في Railway.

    **ملاحظة هامة**: يفضل المستخدم إدارة جميع المتغيرات البيئية، بما في ذلك مفاتيح الـ API، عبر منصة النشر (Railway) لضمان الأمان وسهولة الإدارة.

## 3. عملية البناء والتشغيل

-   سيقوم Railway تلقائياً ببناء مشروعك باستخدام `Dockerfile` الذي قمنا بتوفيره.
-   يتضمن `Dockerfile` خطوات تثبيت `ffmpeg` وجميع مكتبات Python المحددة في `requirements.txt`.
-   بعد اكتمال البناء بنجاح، سيقوم Railway بتشغيل البوت تلقائياً باستخدام الأمر `python main.py`.

## 4. مراقبة البوت

-   **السجلات (Logs)**: يمكنك مراقبة سجلات البوت في تبويب `Logs` في لوحة تحكم Railway للتأكد من أنه يعمل بشكل صحيح ولتشخيص أي مشاكل.
-   **الصحة (Health)**: تأكد من أن حالة الخدمة (Service Status) تظهر باللون الأخضر (Running).

## 5. تحديثات المشروع

-   عند إجراء أي تغييرات على الكود في مستودع GitHub الخاص بك، سيقوم Railway تلقائياً بإعادة البناء والنشر (إذا كان الربط مفعلاً).

بهذه الخطوات، سيكون بوت تلغرام Spotify الخاص بك جاهزاً للعمل على Railway!
