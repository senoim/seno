# 🚂 دليل النشر على Railway.app

دليل كامل خطوة بخطوة لرفع بوت Spotify على Railway مجاناً

---

## 🎯 لماذا Railway؟

✅ **مجاني** - 500 ساعة تشغيل شهرياً (كافية للبوت)  
✅ **سهل الاستخدام** - نشر بضغطة زر واحدة  
✅ **يعمل 24/7** - البوت يعمل دائماً  
✅ **قاعدة بيانات مدمجة** - SQLite يعمل تلقائياً  
✅ **لا يحتاج بطاقة ائتمان** (للخطة المجانية)

---

## 📋 المتطلبات

قبل البدء، تحتاج إلى:

1. ✅ حساب على [GitHub](https://github.com) (مجاني)
2. ✅ حساب على [Railway](https://railway.app) (مجاني)
3. ✅ توكن البوت من [@BotFather](https://t.me/BotFather)
4. ✅ آيديك من [@userinfobot](https://t.me/userinfobot)

---

## 🚀 طريقة النشر (3 طرق)

### 📦 الطريقة 1: النشر المباشر من GitHub (موصى بها ⭐)

#### الخطوة 1: رفع المشروع على GitHub

1. **سجل دخول إلى [GitHub](https://github.com)**

2. **أنشئ repository جديد:**
   - اضغط على `+` في الأعلى → `New repository`
   - اسم المشروع: `spotify-telegram-bot`
   - اختر `Public` أو `Private`
   - اضغط `Create repository`

3. **ارفع الملفات:**

   **الطريقة الأولى - عبر الموقع:**
   - اضغط `uploading an existing file`
   - اسحب جميع ملفات المشروع
   - اضغط `Commit changes`

   **الطريقة الثانية - عبر Git:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/spotify-telegram-bot.git
   git push -u origin main
   ```

#### الخطوة 2: النشر على Railway

1. **اذهب إلى [Railway.app](https://railway.app)**

2. **اضغط `Start a New Project`**

3. **اختر `Deploy from GitHub repo`**

4. **قد يطلب منك ربط حساب GitHub:**
   - اضغط `Configure GitHub App`
   - اختر المشروع `spotify-telegram-bot`
   - اضغط `Install & Authorize`

5. **اختر المشروع من القائمة**

6. **اضغط `Deploy Now`**

7. **انتظر حتى ينتهي البناء** (Build) - عادة 2-3 دقائق

#### الخطوة 3: إضافة المتغيرات البيئية

1. **في صفحة المشروع، اضغط على `Variables`**

2. **أضف المتغيرات التالية واحداً تلو الآخر:**

   ```
   BOT_TOKEN = ضع_توكن_البوت_هنا
   ADMIN_ID = ضع_آيديك_هنا
   CHANNEL_USERNAME = @your_channel
   DATABASE_PATH = bot_database.db
   ```

   **مثال:**
   ```
   BOT_TOKEN = 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ADMIN_ID = 123456789
   CHANNEL_USERNAME = @mychannel
   DATABASE_PATH = bot_database.db
   ```

3. **اضغط `Add` بعد كل متغير**

#### الخطوة 4: إعادة النشر

1. **اضغط على تبويب `Deployments`**

2. **اضغط على القائمة `⋮` بجانب آخر Deployment**

3. **اختر `Redeploy`**

4. **انتظر حتى ينتهي البناء**

#### الخطوة 5: التحقق من التشغيل

1. **اذهب إلى `View Logs` لمشاهدة السجلات**

2. **يجب أن ترى:**
   ```
   🚀 البوت يعمل الآن...
   ✅ تم تهيئة قاعدة البيانات
   ```

3. **افتح البوت على تلغرام وأرسل `/start`**

4. **يجب أن تصلك رسالة الترحيب! ✅**

---

### 🌐 الطريقة 2: النشر من Railway CLI

إذا كنت تفضل استخدام Terminal:

#### 1. تثبيت Railway CLI

**في Linux/Mac:**
```bash
bash <(curl -fsSL cli.new)
```

**في Windows (PowerShell):**
```powershell
iwr -useb https://cli.new | iex
```

#### 2. تسجيل الدخول
```bash
railway login
```

#### 3. النشر
```bash
cd /path/to/spotify-bot
railway init
railway up
```

#### 4. إضافة المتغيرات
```bash
railway variables set BOT_TOKEN="your_token_here"
railway variables set ADMIN_ID="your_id_here"
railway variables set CHANNEL_USERNAME="@your_channel"
railway variables set DATABASE_PATH="bot_database.db"
```

---

### 📤 الطريقة 3: رفع ملف ZIP

1. **اضغط ملفات المشروع في ملف ZIP**

2. **في Railway:**
   - اضغط `New Project`
   - اختر `Empty Project`
   - اضغط `+ New` → `Empty Service`

3. **في الـ Service:**
   - اذهب لـ `Settings`
   - في `Source` اختر `Local Directory`
   - ارفع ملف ZIP

4. **أضف المتغيرات كما في الطريقة الأولى**

---

## ⚙️ إعدادات إضافية (اختيارية)

### 🔄 تفعيل Auto-Deploy

لتحديث البوت تلقائياً عند رفع تغييرات على GitHub:

1. **في Railway → Settings**
2. **فعّل `Watch Paths`**
3. **أضف: `**/*.py`** لمراقبة ملفات Python فقط

### 💾 النسخ الاحتياطي لقاعدة البيانات

Railway يحتفظ بـ SQLite تلقائياً، لكن للأمان:

1. **في Railway → Storage → Volumes**
2. **أنشئ Volume جديد:**
   - Mount Path: `/app/data`
   - Size: 1GB (مجاني)

3. **عدل ملف `.env` في Railway:**
   ```
   DATABASE_PATH = /app/data/bot_database.db
   ```

### 📊 مراقبة الأداء

1. **اذهب إلى `Metrics` في Railway**
2. **شاهد:**
   - استهلاك RAM
   - استهلاك CPU
   - عدد الـ Requests

---

## 🐛 حل المشاكل الشائعة

### ❌ Build Failed

**السبب:** خطأ في ملف `requirements.txt`

**الحل:**
```bash
# تأكد من صحة الإصدارات
pip freeze > requirements.txt
```

### ❌ Application Error

**السبب:** متغيرات البيئة غير موجودة

**الحل:**
1. تحقق من Variables في Railway
2. تأكد من إضافة `BOT_TOKEN` و `ADMIN_ID`
3. أعد النشر

### ❌ البوت لا يستجيب

**السبب:** البوت متوقف أو crashed

**الحل:**
1. راجع Logs في Railway
2. ابحث عن رسائل الخطأ
3. تأكد من صحة التوكن

### ❌ "ModuleNotFoundError"

**السبب:** مكتبة غير مثبتة

**الحل:**
1. أضف المكتبة في `requirements.txt`
2. ارفع التغيير على GitHub
3. Railway سيعيد البناء تلقائياً

### ⚠️ "Out of Hours" (نفاد الساعات)

**السبب:** تجاوزت 500 ساعة شهرياً

**الحلول:**
- **الحل 1:** ترقية الخطة ($5/شهر لـ unlimited)
- **الحل 2:** نشر على منصة أخرى (Render, Heroku)
- **الحل 3:** انتظر بداية الشهر الجديد

---

## 📱 الوصول إلى Logs

لمشاهدة سجلات البوت في الوقت الفعلي:

### من الموقع:
1. افتح مشروعك في Railway
2. اضغط `View Logs`
3. شاهد الرسائل الحية

### من CLI:
```bash
railway logs
```

---

## 🔐 أمان إضافي

### 1. إخفاء المتغيرات الحساسة

✅ **صحيح:** استخدام Variables في Railway  
❌ **خطأ:** كتابة التوكن في الكود

### 2. حماية الـ Repository

إذا كان المشروع Public:
- تأكد من عدم رفع `.env` على GitHub
- استخدم `.gitignore` (موجود في المشروع)

### 3. تحديث التوكن

إذا تسرب التوكن:
1. اذهب لـ @BotFather
2. `/revoke` لإلغاء التوكن القديم
3. `/token` للحصول على توكن جديد
4. حدّث `BOT_TOKEN` في Railway Variables

---

## 📊 مراقبة الاستخدام

### عرض ساعات الاستخدام:
1. اذهب لـ [Railway Dashboard](https://railway.app/account)
2. اضغط `Usage`
3. شاهد استهلاكك الشهري

### تقليل الاستهلاك:
- استخدم `sleep()` في الكود للتطبيقات غير الحرجة
- قلل عدد الـ polling إذا أمكن
- استخدم webhooks بدلاً من polling (متقدم)

---

## 🔄 تحديث البوت

عند إجراء تغييرات على الكود:

### إذا استخدمت GitHub:
1. ارفع التغييرات على GitHub
2. Railway سيعيد النشر تلقائياً ✅

### إذا استخدمت CLI:
```bash
railway up
```

### إذا رفعت ZIP:
1. ارفع ملف ZIP جديد
2. أعد النشر يدوياً

---

## 💡 نصائح احترافية

### 1. استخدام Custom Domain (اختياري)
يمكنك ربط دومين خاص:
1. في Railway → Settings → Domains
2. أضف دومينك
3. اضبط DNS Records

### 2. التنبيهات التلقائية
فعّل التنبيهات عند حدوث أخطاء:
1. Settings → Notifications
2. ربط Discord أو Slack

### 3. Staging Environment
أنشئ بيئة تجريبية:
1. انسخ المشروع
2. استخدمه للاختبار قبل النشر الفعلي

---

## 📞 الحصول على المساعدة

### مشاكل في Railway:
- [Railway Discord](https://discord.gg/railway)
- [Railway Docs](https://docs.railway.app)
- [Railway Status](https://status.railway.app)

### مشاكل في البوت:
- راجع `README.md` و `SETUP_GUIDE.md`
- افتح Issue على GitHub
- تحقق من Logs

---

## 🎉 تهانينا!

بوتك الآن يعمل على Railway ومتاح 24/7! 🚀

### الخطوات التالية:
✅ اختبر جميع مميزات البوت  
✅ شارك رابط البوت مع الأصدقاء  
✅ راقب الإحصائيات من `/admin`  
✅ استمتع! 🎵

---

**آخر تحديث:** فبراير 2026  
**المنصة:** Railway.app  
**الإصدار:** 1.0
