# 🚀 دليل الإعداد السريع

دليل خطوة بخطوة لتشغيل بوت Spotify Downloader

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من توفر:
- ✅ حاسوب بنظام Windows / Linux / macOS
- ✅ Python 3.9 أو أحدث ([تحميل Python](https://www.python.org/downloads/))
- ✅ حساب تلغرام

---

## 🔧 الخطوة 1: تحميل المشروع

### الطريقة الأولى: عبر Git
```bash
git clone https://github.com/your-username/spotify-bot.git
cd spotify-bot
```

### الطريقة الثانية: التحميل المباشر
1. حمّل الملفات كملف ZIP
2. فك الضغط عن الملف
3. افتح Terminal/CMD في مجلد المشروع

---

## 📦 الخطوة 2: تثبيت المكتبات

افتح Terminal/CMD في مجلد المشروع وشغّل:

```bash
pip install -r requirements.txt
```

**ملاحظة للويندوز:** إذا لم يعمل الأمر، جرب:
```bash
python -m pip install -r requirements.txt
```

---

## 🤖 الخطوة 3: إنشاء بوت تلغرام

1. **افتح محادثة مع [@BotFather](https://t.me/BotFather)**

2. **أرسل الأمر:**
   ```
   /newbot
   ```

3. **اختر اسم للبوت** (مثال: My Spotify Bot)

4. **اختر معرف للبوت** (يجب أن ينتهي بـ `bot`)
   - مثال: `myspotify_bot`
   - مثال: `MyMusicDownloadBot`

5. **انسخ التوكن** الذي سيرسله لك BotFather
   - يكون بهذا الشكل: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

---

## 🆔 الخطوة 4: الحصول على آيدي التلغرام

1. **افتح محادثة مع [@userinfobot](https://t.me/userinfobot)**

2. **اضغط على `/start`**

3. **انسخ الرقم** الذي يظهر أمام "Id:"
   - مثال: `123456789`

---

## 📢 الخطوة 5: إنشاء قناة (اختياري)

1. **في تلغرام، اضغط على قائمة → قناة جديدة**

2. **اختر اسم للقناة** (مثال: My Music Channel)

3. **اجعلها قناة عامة واختر معرف**
   - مثال: `@mymusicchannel`

4. **أضف البوت كمسؤول في القناة:**
   - اذهب لإعدادات القناة
   - المسؤولون → إضافة مسؤول
   - ابحث عن البوت وأضفه
   - **مهم جداً:** فعّل صلاحية "نشر الرسائل"

---

## ⚙️ الخطوة 6: إعداد ملف البيئة

1. **انسخ ملف `.env.example` إلى `.env`**

   **في Windows (CMD):**
   ```cmd
   copy .env.example .env
   ```

   **في Linux/Mac (Terminal):**
   ```bash
   cp .env.example .env
   ```

2. **افتح ملف `.env` بأي محرر نصوص**

3. **املأ البيانات:**

   ```env
   # ضع التوكن هنا (من BotFather)
   BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   
   # ضع آيديك هنا (من userinfobot)
   ADMIN_ID=123456789
   
   # ضع معرف قناتك هنا (أو اتركه كما هو وغيره لاحقاً)
   CHANNEL_USERNAME=@mymusicchannel
   
   # اسم ملف قاعدة البيانات (اتركه كما هو)
   DATABASE_PATH=bot_database.db
   ```

4. **احفظ الملف**

---

## ▶️ الخطوة 7: تشغيل البوت

في Terminal/CMD، شغّل الأمر:

```bash
python bot.py
```

**يجب أن تظهر هذه الرسائل:**
```
🚀 البوت يعمل الآن...
✅ تم تهيئة قاعدة البيانات
```

**وستصلك رسالة من البوت:**
```
✅ البوت يعمل الآن!

يمكنك استخدام /admin للوصول إلى لوحة التحكم
```

---

## ✅ الخطوة 8: اختبار البوت

1. **افتح محادثة مع البوت في تلغرام**

2. **اضغط `/start`**

3. **للوصول للوحة التحكم، أرسل:**
   ```
   /admin
   ```

4. **جرب جميع الأزرار للتأكد من عملها!**

---

## 🎵 (اختياري) تفعيل التحميل من Spotify

لتفعيل ميزة التحميل الفعلي:

### 1. تثبيت spotdl
```bash
pip install spotdl
```

### 2. الحصول على Spotify API Keys

1. **سجل في [Spotify Developer](https://developer.spotify.com/dashboard)**

2. **أنشئ تطبيق جديد** واختر اسم مناسب

3. **انسخ `Client ID` و `Client Secret`**

### 3. تعديل الكود

افتح ملف `handlers.py` وابحث عن السطر:
```python
# from spotdl import Spotdl
```

وفعّل الكود كما هو موضح في الملف.

---

## 🔄 إبقاء البوت يعمل باستمرار

### الطريقة الأولى: استخدام screen (Linux)
```bash
screen -S spotify-bot
python bot.py
# اضغط Ctrl+A ثم D للخروج
# للعودة: screen -r spotify-bot
```

### الطريقة الثانية: استخدام nohup (Linux/Mac)
```bash
nohup python bot.py &
```

### الطريقة الثالثة: خدمة Systemd (Linux)
أنشئ ملف `/etc/systemd/system/spotify-bot.service`:
```ini
[Unit]
Description=Spotify Download Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/spotify-bot
ExecStart=/usr/bin/python3 bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

ثم:
```bash
sudo systemctl enable spotify-bot
sudo systemctl start spotify-bot
```

### الطريقة الرابعة: استضافة سحابية
- **Heroku** (مجاني لحد معين)
- **Railway** (مجاني لحد معين)
- **PythonAnywhere** (مجاني لحد معين)
- **VPS** (DigitalOcean, Linode, Vultr)

---

## ❓ حل المشاكل الشائعة

### ❌ "No module named 'aiogram'"
**الحل:**
```bash
pip install aiogram
```

### ❌ "Unauthorized" عند تشغيل البوت
**الحل:** تحقق من التوكن في ملف `.env`

### ❌ البوت لا يرد على /start
**الحل:**
1. تأكد من أن البوت يعمل
2. أرسل `/start` في محادثة خاصة مع البوت (ليس في مجموعة)

### ❌ "You are not subscribed" رغم الاشتراك
**الحل:**
1. تأكد أن البوت مسؤول في القناة
2. تأكد من تفعيل صلاحية "نشر الرسائل" للبوت في القناة
3. تحقق من صحة `CHANNEL_USERNAME` في ملف `.env`

### ❌ الإشعارات لا تصل
**الحل:**
1. أرسل `/start` للبوت أولاً (هذا مهم!)
2. تحقق من صحة `ADMIN_ID` في ملف `.env`
3. فعّل الإشعارات من `/admin` → إعدادات الإشعارات

---

## 📞 الحصول على المساعدة

إذا واجهت مشكلة:

1. **راجع README.md** للشرح التفصيلي
2. **ابحث في Issues على GitHub**
3. **افتح Issue جديد** مع وصف المشكلة
4. **تواصل مع المطور**

---

## 🎉 تهانينا!

بوتك الآن يعمل بنجاح! 🚀

استمتع بإدارة البوت واستخدامه 🎵

---

**آخر تحديث:** فبراير 2026
